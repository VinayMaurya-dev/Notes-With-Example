// wap to convert given hours to the minutes.
#include<stdio.h>
void main(){
	int h,m;
	printf("Enter a value in hours form to convert into minutes:");
	scanf("%d",&h);
	printf("%d hours is = %d minutes",h,h*60);
}
